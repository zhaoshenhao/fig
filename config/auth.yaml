# API 鉴权配置
#
# api_keys: 有效的 API key 列表（支持 ${ENV_VAR}）。空列表 = 不启用鉴权。
# skip_paths: 跳过鉴权的路径前缀列表。
#
# 客户端请求时在 Header 中携带:
#   X-API-Key: <key>

api_keys:
  - "${KF_API_KEY}"

skip_paths:
  - /health
  - /ready
  - /docs
  - /openapi.json
  - /redoc
