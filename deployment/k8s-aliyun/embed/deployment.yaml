apiVersion: apps/v1
kind: Deployment
metadata:
  name: kf-embed
  namespace: <NAMESPACE>
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kf-embed
  template:
    metadata:
      labels:
        app: kf-embed
    spec:
      imagePullSecrets:
        - name: regsecret
      containers:
        - name: kf-embed
          # 模型在构建期预下载到 /opt/fastembed_cache，运行期零外网依赖
          image: <ACR_REGISTRY>/kf-embed:<EMBED_IMAGE_TAG>
          ports:
            - containerPort: 8100
          env:
            - name: EMBED_MODEL
              value: "nomic-ai/nomic-embed-text-v1.5-Q"
            - name: FASTEMBED_CACHE_PATH
              value: "/opt/fastembed_cache"
            - name: EMBED_CACHE_DIR
              value: "/opt/fastembed_cache"
            - name: EMBED_WARMUP
              value: "1"
            # 限制 ONNX 线程数，控制内存
            - name: OMP_NUM_THREADS
              value: "2"
            - name: OMP_WAIT_POLICY
              value: "PASSIVE"
            # 内部服务鉴权 Key（与 kf-api 共享，从 Secret 注入）
            - name: EMBED_API_KEY
              valueFrom:
                secretKeyRef:
                  name: kf-secrets
                  key: EMBED_API_KEY
          resources:
            requests:
              memory: "128Mi"
            limits:
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8100
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /ready
              port: 8100
            initialDelaySeconds: 5
            periodSeconds: 5
